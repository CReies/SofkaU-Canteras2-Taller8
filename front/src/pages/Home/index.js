import Contacts from '../../components/Contacts';
import './Home.css';
/**
 * Main page of the app
 *
 * @returns Main page
 */
const Home = () => {
	return (
		<>
			<section>
				<Contacts />
			</section>
		</>
	);
};

export default Home;
